$kind: http-request
url: "{{baseUrl}}/api/loans?lenderId={{lenderId}}&borrowerId={{borrowerId}}&amount=5000"
method: POST
headers:
  Authorization: Bearer {{token}}
queryParams:
  lenderId: "{{lenderId}}"
  borrowerId: "{{borrowerId}}"
  amount: "5000"
order: 1000
